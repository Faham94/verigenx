class {{ dut_name }}_test_base extends uvm_test;

    {{ dut_name }}_env env;

    `uvm_component_utils({{ dut_name }}_test_base)

    function new(string name = "{{ dut_name }}_test_base", uvm_component parent = null);
        super.new(name, parent);
    endfunction

    virtual function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        env = {{ dut_name }}_env::type_id::create("env", this);
    endfunction

    virtual task run_phase(uvm_phase phase);
        // {% llm_fill "test_base_run" %}
        // {% endllm_fill %}
    endtask

endclass
